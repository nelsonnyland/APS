@page
@model APS.Pages.Views.InvoicesModel

@{
    ViewData["Title"] = "Invoices";
}

<h1>Invoices</h1>

@*<p>
    <a asp-page="Create">Create New</a>
</p>*@
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Invoices[0].VendorID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Invoices[0].InvoiceDesc)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Invoices[0].InvoiceDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Invoices[0].InvoiceTotal)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Invoices) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.VendorID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.InvoiceDesc)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.InvoiceDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.InvoiceTotal)
            </td>
            @*<td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>*@
        </tr>
}
    </tbody>
</table>
