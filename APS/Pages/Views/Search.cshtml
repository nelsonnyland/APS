@page
@using APS.Pages
@model APS.Pages.Views.SearchModel

@{
    ViewData["Title"] = "Search";
}

<h1>Search</h1>

<div class="container">
    <div class="row">
        <form asp-page="Search" method="get">
            <div class="form-actions no-color">
                <p>
                    Search:
                    <input type="text" name="SearchString" value="@Model.CurrentFilter" />
                    <input type="submit" value="Search" class="btn btn-primary" /> | 
                    <a asp-page="/Views/Search">Clear</a>
                </p>
            </div>
        </form>
    </div>
    @if (Model.SearchVM != null)
    {
        @if (Model.SearchVM.Parts != null && Model.SearchVM.Parts.Count > 0)
        {
            <h3>Parts</h3>
            <table class="table">
                <thead>
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.SearchVM.Parts[0].PartName)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.SearchVM.Parts[0].PartDesc)
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.SearchVM.Parts)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.PartName)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.PartDesc)
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }
        @if (Model.SearchVM.Vehicles != null && Model.SearchVM.Vehicles.Count > 0)
        {
            <h3>Vehicles</h3>
            <table class="table">
                <thead>
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.SearchVM.Vehicles[0].VehMake)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.SearchVM.Vehicles[0].VehModel)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.SearchVM.Vehicles[0].VehYear)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.SearchVM.Vehicles[0].VehEngine)
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.SearchVM.Vehicles)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.VehMake)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.VehModel)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.VehYear)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.VehEngine)
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }
        @if (Model.SearchVM.Stock != null && Model.SearchVM.Stock.Count > 0)
        {
            <h3>Stock</h3>
            <table class="table">
                <thead>
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.SearchVM.Stock[0].Qty)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.SearchVM.Stock[0].Price)
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.SearchVM.Stock)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.Qty)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Price)
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }
        @if (Model.SearchVM.Merchandise != null && Model.SearchVM.Merchandise.Count > 0)
        {
            <h3>Merchandise</h3>
            <table class="table">
                <thead>
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.SearchVM.Merchandise[0].MerchName)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.SearchVM.Merchandise[0].MerchDesc)
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.SearchVM.Merchandise)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.MerchName)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.MerchDesc)
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }
        @if (Model.SearchVM.Vendors != null && Model.SearchVM.Vendors.Count > 0)
        {
            <h3>Vendors</h3>
            <table class="table">
                <thead>
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.SearchVM.Vendors[0].VendorName)
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.SearchVM.Vendors)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.VendorName)
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }
        @if (Model.SearchVM.Invoices != null && Model.SearchVM.Invoices.Count > 0)
        {
            <h3>Invoices</h3>
            <table class="table">
                <thead>
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.SearchVM.Invoices[0].InvoiceDesc)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.SearchVM.Invoices[0].InvoiceDate)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.SearchVM.Invoices[0].InvoiceTotal)
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.SearchVM.Invoices)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.InvoiceDesc)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.InvoiceDate)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.InvoiceTotal)
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }
        @if (Model.SearchVM.Locations != null && Model.SearchVM.Locations.Count > 0)
        {
            <table class="table">
                <thead>
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.SearchVM.Locations[0].LocName)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.SearchVM.Locations[0].LocStreet)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.SearchVM.Locations[0].LocCity)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.SearchVM.Locations[0].LocState)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.SearchVM.Locations[0].LocCode)
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.SearchVM.Locations)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.LocName)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.LocStreet)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.LocCity)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.LocState)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.LocCode)
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }
        @if (Model.SearchVM.Employees != null && Model.SearchVM.Employees.Count > 0)
        {
            <table class="table">
                <thead>
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.SearchVM.Employees[0].EmpFName)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.SearchVM.Employees[0].EmpLName)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.SearchVM.Employees[0].EmpPhone)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.SearchVM.Employees[0].EmpStreet)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.SearchVM.Employees[0].EmpCity)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.SearchVM.Employees[0].EmpState)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.SearchVM.Employees[0].EmpCode)
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.SearchVM.Employees)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.EmpFName)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.EmpLName)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.EmpPhone)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.EmpStreet)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.EmpCity)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.EmpState)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.EmpCode)
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }
    }

</div>