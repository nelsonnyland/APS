@page
@model APS.Pages.Views.PartsModel

@{
    ViewData["Title"] = "Parts";
}

<h1>Parts</h1>

@*<p>
    <a asp-page="Create">Create New</a>
</p>*@
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Parts[0].VehID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Parts[0].VendorID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Parts[0].StockID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Parts[0].PartName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Parts[0].PartDesc)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Parts) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.VehID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.VendorID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StockID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PartName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PartDesc)
            </td>
@*            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>*@
        </tr>
}
    </tbody>
</table>
