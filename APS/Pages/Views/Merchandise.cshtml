@page
@model APS.Pages.Views.MerchandiseModel

@{
    ViewData["Title"] = "Merchandise";
}

<h1>Merchandise</h1>

@*<p>
    <a asp-page="Create">Create New</a>
</p>*@
<table class="table">
    <thead>
        <tr>
            @*<th>
                @Html.DisplayNameFor(model => model.Merchandise[0].VendorID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Merchandise[0].StockID)
            </th>*@
            <th>
                @Html.DisplayNameFor(model => model.Merchandise[0].MerchName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Merchandise[0].MerchDesc)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Merchandise) {
        <tr>
            @*<td>
                @Html.DisplayFor(modelItem => item.VendorID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StockID)
            </td>*@
            <td>
                @Html.DisplayFor(modelItem => item.MerchName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MerchDesc)
            </td>
            @*<td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>*@
        </tr>
}
    </tbody>
</table>
